using UnityEngine;

public class exerciciorepetição01 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        for (int i = 5; i > 0; i--)
        {
            print(i);

        }
        print("Início da partida!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
